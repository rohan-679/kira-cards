// ***********************************************tilt effect****************************

// card 1
VanillaTilt.init(document.querySelector(".card-first"), {
  max: 20,
  speed: 900,
});

// It also supports NodeList
VanillaTilt.init(document.querySelectorAll(".card-first"));

// card 2
VanillaTilt.init(document.querySelector(".card-second"), {
  max: 20,
  speed: 900,
});

// It also supports NodeList
VanillaTilt.init(document.querySelectorAll(".card-second"));

// card 3
VanillaTilt.init(document.querySelector(".card-third"), {
  max: 20,
  speed: 900,
});

// It also supports NodeList
VanillaTilt.init(document.querySelectorAll(".card-third"));

// ***********************************************tilt effect****************************
